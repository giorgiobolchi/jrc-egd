

- llama3.3 model still gives me time out error, so i decided to make an alternative pipeline where I feed in the prompt only two thematic areas at a time and make the interconnections between those. So in total there are 2x21 = 42 pairs of thematic areas, I kept the duplicates (eg TA1-TA3 and TA3-TA1) to ensure directionality of the connections. I also asked the AI to define the connection type (positive or negative, + or -). 
	- → llama3.3 doesnt work still
	- gpt-4o seems to give at least some results 
		- only positive connection, strange no? shall I specify something differently in the prompt?
		- ```RateLimitError: Error code: 429 - {'detail': 'You have exceeded the rate limit for requests. Please, retry later.'}``` → add a 1 minute pause between prompt requests
		- tried again but: ```RateLimitError: Error code: 429 - {'detail': 'You have exceeded your daily quota.'}```oof


- tried to use 'nous-hermes-2-mixtral-8x7b-dpo' model but got the error: ```UnprocessableEntityError: Error code: 422 - {'detail': 'Input validation error: `inputs` tokens + `max_new_tokens` must be <= 32768. Given: 128910 `inputs` tokens and 0 `max_new_tokens`'} ``` 
	- apparently I input *128910*  tokens so maybe i do exceed the token limits after all. Idk what kind of encoder the nous-hermes model uses, but if it is the same for the llama3.3, then I am above the limit of 120k.
	- → try to shorten report1? → deleted bibliography → report1_trimmed
	- → ran nous-hermes again:  ```UnprocessableEntityError: Error code: 422 - {'detail': 'Input validation error: `inputs` tokens + `max_new_tokens` must be <= 32768. Given: 100499 `inputs` tokens and 0 `max_new_tokens`'} ``` *100499* tokens input, should fit below 120k

-  try llama3.3 again with report1_trimmed instead of full report1
	- **It works!** so probably it was the token limit.
	- Comments & **problems**:
		- the justification column is almost always not-added → need to specify in the prompt that it is mandatory
		- the few connections i peeped into did not make directly so much sense tbh
		- mostly positive connection and few negatives ones
		- some empty targets are also assigned to other targets, this should not be possible