

- Els brunch
- finished generating missing pairs from [[2025-04-17]] by shortening chapters 4 and 5 (removed reference list)



ideas for today:
- generate target_to_target network
- while it generates, start reviewing subtheme_to_subtheme network



### PtP network generation

(**PtP** = policydoc_to_policydoc)


fuck i just realised that when i create dictionrary of target data, the target content is often cropped like '*Biodiversity conservation and restoration of n...*' with ***'...'*** at the end.. I need to find a solution to this, and probably also re-generate the *subtheme_to_subtheme* network..

→ gpt@jrc redefined how I import my target data, and added '*.to_dict(orient='records')*' and somehow now it works, all the text is included.

```python
for policydoc in data['policy_document_short'].unique(): # Iterate over the unique 'policy_document_short' values

policydoc_df = data[data['policy_document_short'] == policydoc][['target_code', 'target_content', 'target_assessment']].to_dict(orient='records') # Filter the DataFrame for the current 'policy_document_short' and convert to dictionary

policydoc_dict[policydoc] = policydoc_df # Add the filtered DataFrame to the dictionary


```

But then, the prompt was way too long! So I shortened all chapters that I use by removing the bibliographies  (chapter_2,3,4,5,7 : cut out the bibliographies to save tokens)

→  it greatly reduced amount of tokens and the generation worked
```python 
- Report n°2: {report2['chapter1_short']} + {report2['chapter2_short']} + {report2['chapter3_short']} + {report2['chapter4_short']} + {report2['chapter5_short']} ] + {report2['chapter7_short']} ].
```


